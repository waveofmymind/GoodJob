spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  config:
    activate:
      on-profile: test

jasypt:
  encryptor:
    password: ${JASYPT_PASSWORD}
    bean: jasyptStringEncryptor