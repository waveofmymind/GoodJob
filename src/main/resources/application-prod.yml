spring:
  datasource:
    url: {DB_URL}
    username: {DB_USERNAME}
    password: {DB_PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.com.goodjob: debug #

openai:
  token: {OPENAI_API_KEY}
