package com.goodjob.domain.resume.repository;

import com.goodjob.domain.resume.domain.ExpectedQuestion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExpectedQuestionJpaRepository extends JpaRepository<ExpectedQuestion,Long> {


}
